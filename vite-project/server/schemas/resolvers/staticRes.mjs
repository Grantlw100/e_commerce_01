// create static resolvers for 
    //creating tokens and refreshing tokens
    // handling uploads, resizing or tagging files during a createEntity or updateEntity mutation
    // user registrations that create associated records in multiple collections 
    // transaction-based operations like purchases, deliveries, and refunds
    // system diagnostics or maintenance queries that return system information


const StaticResolvers = {
    Query: {
    },
    Mutation: {
    }
};